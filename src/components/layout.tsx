import { css } from 'styled-components';

const layout = {
    container: css`
        margin: 0 auto 600px auto;
        max-width: 1140px;
        position: relative;
    `,
};

export default layout;

import React from 'react';
import { storiesOf } from '@storybook/react';
import FooterContainer from '../footer.container';

storiesOf('Footer Block', module).add('Default', () => <FooterContainer />);
